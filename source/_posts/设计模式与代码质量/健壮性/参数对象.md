```js
// 必传参数：  el template
// 如何保证必传值不为空： 提供一个默认配置，把new时的入参与默认配置做合并
function Vue(config){
  let _default = {
    el: document, 
    template:'<div></div>'
  }
  for(let key in config){
    _default[key] = config[key] || _default[key];
  }
}
```